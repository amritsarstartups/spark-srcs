"use client"
import React, { useEffect, useState } from 'react'

function LocationDetails({params}) {
  const resolvedParams = React.use(params);

  return (
    <div>
      <h1>Location Details</h1>
    </div>
  )
}

export default LocationDetails
